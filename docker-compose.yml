services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ayce-event-fronted:latest
    env_file:
      - .env.production
    ports:
      - '3001:3000'
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
